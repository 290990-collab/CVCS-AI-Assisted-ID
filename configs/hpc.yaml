mode: preprocess

data:
  raw_root: "/scratch/datasets/floorplan/raw"
  processed_root: "/scratch/datasets/floorplan/processed"
  batch_size: 64

model:
  in_dim: 16
  hidden_dim: 256
  num_layers: 6
  embedding_dim: 128

training:
  lr: 0.0005
  epochs: 20
  device: cuda